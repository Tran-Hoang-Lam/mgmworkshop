apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: rabbit-dashboard
  namespace: demo
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`demo-rabbit-dashboard.mgmworkshop.com`)
      kind: Rule
      services:
        - name: rabbit-mq
          port: 15672
  tls:
    certResolver: default
